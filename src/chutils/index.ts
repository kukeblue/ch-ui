import chHooks from './chHooks';
import Ajax from './request';
import chFormats from './chFormats';
import chValidator from './chValidator';
import chCache from './cache';

const ChUtils = {
  chHooks,
  chFormats,
  Ajax,
  chValidator,
  chCache,
};

export default ChUtils;
